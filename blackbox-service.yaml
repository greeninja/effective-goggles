---
apiVersion: v1
kind: Service
metadata:
  name: blackbox-exporter
  namespace: monitoring-tools
  labels:
    app: blackbox-exporter
spec:
  type: ClusterIP
  ports:
    - port: 9115
      targetPort: http
      name: http
  selector:
    app: blackbox-exporter
